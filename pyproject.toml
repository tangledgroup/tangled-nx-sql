[project]
name = "tangled-nx-sql"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "networkx[default]>=3.6.1",
    "sqlalchemy>=2.0.49",
]

[dependency-groups]
dev = [
    "pytest-aiohttp>=1.1.0",
    "ruff>=0.15.11",
    "ty>=0.0.31",
]
